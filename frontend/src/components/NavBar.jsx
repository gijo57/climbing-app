import Drawer from '@mui/material/Drawer';

const NavBar = () => {
  return (
    <Drawer variant="persistent" open>
      <ul>
        <li>j</li>
      </ul>
    </Drawer>
  );
};

export default NavBar;
